{
  "name": "paragliding-agent",
  "version": "1.0.0",
  "description": "Daily paragliding conditions brief via Claude",
  "scripts": {
    "build": "tsc",
    "start": "node dist/run_ci.js",
    "dev": "tsx run_local.ts",
    "dev:dry": "tsx run_local.ts --dry-run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "dotenv": "^16.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}